"""Day 19: Encapsulation Polymorphism"""

# class task 1: 
    **Create a bank account class with :**
    - A public account holder name
    - A protected account type
    - A private PIN and balance
    
    **Create a SavingAccount class that adds 5% interest to the balance using methods**
# isinstance() => use of isinstance()

# class task 2:
    **Create an abstract BankAccount class with abstract methods deposit() and withdraw(). Implement them in a SavingAccount class**

# Polymorphism
    => same name but different works
    => use in real-world
    => Examples:
        - method overloading (using **args)
        - method overriding (using same function name but task different)
        - operator overloading 
        - duck typing
    => Types:
        - built-in polymorphism :
            # functional polymorpshim => eg: len()
            # opeartor polymorphism => eg: + (performs both string concatenation and sum also for numeric data)
        - runtime polymorphism (Method overriding)
        - compiletime polymorphism (Method overloading)
        - opeartor overloadin
    => Example



# syntactic sugar